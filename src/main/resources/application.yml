server:
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/coffee_market_db
#  jpa:
#    hibernate:
#      ddl-auto: create

security:
  basic:
    path: /admin
    authorize-mode: authenticated
  user:
    name: root
    password: root


